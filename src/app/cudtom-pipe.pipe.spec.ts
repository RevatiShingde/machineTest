import { CudtomPipePipe } from './cudtom-pipe.pipe';

describe('CudtomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CudtomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
